[project]
version = "0.1.0"
name = "ShaperCutout"
requires-python = ">=3.11"
dependencies = []
description = "FreeCAD workbench for building objects out of plywood cut sheets, designed for the Shaper Origin CNC mill."

[project.optional-dependencies]
dev = [
    "freecad-stubs>=1.0.21",
    "pyside6>=6.11.0",
]
